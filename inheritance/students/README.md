# Inheritance

This version ended up making the `Student` class an abstract class. The Main class is now able to load all student information from the input file. Moreover, it is capable of storing both `GraduateStudent` and `UndergraduateStudent` objects into a single array container.
